# Copy this file to ".env" and paste your Cloudflare Tunnel token below.
# Dashboard: Zero Trust > Networks > Tunnels > (your tunnel) > Install connector
# — copy the long token from the `--token` value.
TUNNEL_TOKEN=
